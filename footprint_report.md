# Footprint Report

> Live `du -sh tutor/` and per-component breakdown. Updated after every model swap.

## Live total

```
$ du -sh tutor/
TBD  tutor/
```

## Per-component target vs actual

| Component                       | Target  | Actual | Notes                                  |
|---------------------------------|---------|--------|----------------------------------------|
| `tutor/model.gguf` (TinyLlama)  | ≤ 30 MB | TBD    | Q4_K_M, merged QLoRA adapter           |
| `tutor/whisper_int8.onnx`       | ≤ 25 MB | TBD    | int8 dynamic-quantised whisper-tiny    |
| `tutor/owlvit_tiny.onnx`        | ≤ 15 MB | TBD    | Optional; falls back to OpenCV blob    |
| `tutor/dkt.pt`                  | < 1 MB  | TBD    | Tiny GRU, 32-dim hidden                |
| `tutor/*.py` source             | < 1 MB  | TBD    | Pure Python, no compiled extensions    |
| **Total tutor/**                | ≤ 75 MB | TBD    |                                        |

## Excluded by design (per brief)

- `tts_cache/` — Piper TTS rendered prompts (regenerated on first launch).
- `data/T3.1_Math_Tutor/` — curriculum JSON + seeds, regenerated by `generate_curriculum.py`.
- `assets/` — rendered scene images for visual items.

## Reproduce the measurement

```bash
du -sh tutor/
du -sh tutor/* | sort -h
```
